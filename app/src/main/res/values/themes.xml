<resources>
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here -->
        <item name="android:windowBackground">@color/white</item> <!-- Light background -->
        <item name="android:textColor">@color/black</item> <!-- Light text color -->
    </style>
</resources>
